# In python it is often asked question
x = 10
y = 20
x, y = y, x
# this is same as
x, y = (20, 10)
# when we write comma separated values/variables of right of '=' and some variables on left
# we are basically defining tuple on right
# and unpacking it on left
a, b = 10, 20